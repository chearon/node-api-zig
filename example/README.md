This is the callbacks example from [`node-addon-examples`](https://github.com/nodejs/node-addon-examples). Run it like this:

1. `zig build`
2. `node addon.js`

Use the build.zig in this directory as a starting point
